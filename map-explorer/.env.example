# TFD Map Explorer — env

# Production vector tiles (no protocol). Replaces YOUR_TILE_HOST in style JSON.
# Example: tiles.tefedila.com
# VITE_TILE_HOST=

# Without VITE_TILE_HOST the app runs the real TFD light/dark themes
# in demo mode (local GeoJSON + theme paints).
